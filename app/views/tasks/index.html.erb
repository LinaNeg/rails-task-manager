<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= task.complited %> <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>
<p><%= link_to 'Ajouter une tâche', tasks_new_path %></p>
